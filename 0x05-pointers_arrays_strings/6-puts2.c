#include "main.h"
#include <string.h>

/**
 * puts2 - puts2 function for the task reversing string
 * @str:  str parameter
 */

void puts2(char *str)
{
	int a;

	for (a =0; s[a] != '\0';a++0)
	{
		if (a % 2 == 0)
		{
			-putchar(s[a]);
		}
	}
	_putchar('\n');
}
